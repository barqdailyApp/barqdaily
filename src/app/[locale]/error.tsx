"use client";

import { View500 } from "@/sections/error";

// ----------------------------------------------------------------------

export const metadata = {
  title: "Error page!",
};

export default function NotFoundPage() {
  return <View500 />;
}
